{
    "name": "hexapod",
    "version": "0.1.0",
    "private": true,
    "dependencies": {
        "express": "^4.18.2",
        "hexapod-kinematics-library": "0.0.5",
        "johnny-five": "^2.1.0",
        "plotly.js-gl3d-dist-min": "^1.54.7",
        "react": "^16.13.1",
        "react-dom": "^16.13.1",
        "react-icons": "^3.10.0",
        "react-plotly.js": "^2.4.0",
        "react-router-dom": "^5.2.0",
        "react-scripts": "^3.0.1",
        "serialport": "^12.0.0",
        "socket.io": "^4.7.2",
        "socket.io-client": "^3.0.5"
    },
    "scripts": {
        "dev:client": "SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts start",
        "build:client": " SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts build",
        "prod:client": "npm run build:client && serve -s build",
        "run:server": "node ./src/_ROBOT_SERVER.js",
        "eject": "react-scripts eject",
        "start": " SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts start",
        "build": " SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts build"
    
    },
    "eslintConfig": {
        "extends": "react-app"
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    },
    "devDependencies": {
        "prettier": "2.0.5"
    }
}
